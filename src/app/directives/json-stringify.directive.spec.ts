import { JsonStringifyDirective } from './json-stringify.directive';

describe('JsonStringifyDirective', () => {
  it('should create an instance', () => {
    const directive = new JsonStringifyDirective();
    expect(directive).toBeTruthy();
  });
});
